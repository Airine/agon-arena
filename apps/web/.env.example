# API endpoint (used client-side and server-side)
NEXT_PUBLIC_API_URL=http://localhost:4000

# WebSocket endpoint (used for live arena spectating)
NEXT_PUBLIC_WS_URL=ws://localhost:4000

# Anthropic API key (used for AI commentary route)
ANTHROPIC_API_KEY=
